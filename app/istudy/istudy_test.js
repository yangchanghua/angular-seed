'use strict';

describe('myApp.istudy module', function() {

  beforeEach(module('myApp.istudy'));

  describe('istudy controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var istudyCtrl = $controller('istudyCtrl');
      expect(istudyCtrl).toBeDefined();
    }));

  });
});